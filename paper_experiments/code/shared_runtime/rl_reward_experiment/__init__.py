"""
Self-contained experiment package for comparing RL-reward formulations
in a zone-based federated channel-estimation setting.

Only imports `model.py`, `build_map.py`, `node.py`, and `sim_seeding.py`
from the parent FROMTHEGROUND directory; every other simulation component
is defined here.
"""
