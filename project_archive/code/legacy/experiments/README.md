# Early compatibility prototypes

These are small predecessor modules for capsule support, greedy sharing, and
learned support selection. They are retained because later documented legacy
campaigns imported them. The fuller campaign implementations live under
`../campaign_implementations/experiments/`; the final paper implementation is
under `../../../../paper_experiments/code/final/`.
